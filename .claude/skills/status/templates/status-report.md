# 현재 상태
_{generated_at} 기준_

## 최근 작업
{최근 커밋·상태변경에서 핵심 1~3줄: `- {id} {한 일} → {현재 상태}`}

## 진행 중인 작업
{status가 in-progress인 마일스톤을 기능→마일스톤→task 3단 트리로:}
{`**{feature}** (기능)`}
{`└ {board의 display 줄 그대로}` — 예(가상): `M-S-99 · 예시 마일스톤  ▓▓▓░░░░░░░ 1/3`}
{`    ├ {task} `[{session}]` {요약} — {status}{ · PR {pr}}` (마지막은 └)}
{없으면: `진행 중인 작업 없음`}

## 대기 / 후보
{미착수(backlog) 기능·후보 마일스톤을 같은 3단 트리로. 후보 마일스톤은 `(후보)` 표기:}
{없으면: `없음`}

## 미해결 이슈
{status가 done이 아닌 issue: `- {id} {요약} · {status}`}
{없으면: `없음`}

## 확인할 것 (drift)
{git·gh 대조에서 어긋난 것만. 없으면 이 섹션째 빼기:
- `{id} — PR 머지됐는데 파일은 아직 {status}`
- `{branch} — 브랜치만 있고 spec이 없어요`}

──────────
**다음 액션**
{현재 상태에 맞는 다음 스킬 1~3개: `` `/{skill}` {한 줄} ``}

_진행바 · █ 완료  ▓ 진행  ░ 대기_
