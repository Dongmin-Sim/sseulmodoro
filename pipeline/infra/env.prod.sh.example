# copy to env.prod.sh (gitignored) and fill in real values
PROJECT_ID="your-prod-project-id"        # globally unique across all GCP
PROJECT_NAME="your-prod project name"
REPOSITORY="repo-name"
LOCATION="us-central1"                    # Artifact Registry / Cloud Run region
BQ_LOCATION="US"                          # BigQuery dataset location
WITH_SCHEDULER=true                       # prod runs unattended weekly
BILLING_ACCOUNT=""
