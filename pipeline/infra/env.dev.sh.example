# copy to env.dev.sh (gitignored) and fill in real values
PROJECT_ID="your-dev-project-id"         # globally unique across all GCP
PROJECT_NAME="your-dev project name"
REPOSITORY="repo-name"
LOCATION="us-central1"                    # Artifact Registry / Cloud Run region
BQ_LOCATION="US"                          # BigQuery dataset location
WITH_SCHEDULER=false                      # dev runs via CD/manual, no weekly cron
BILLING_ACCOUNT=""
